#include "main.h"

/**
* _islower - This function for check lowercase character.
* @c: the character to check.
* Return: 1 if c is lowercase, 0 otherwise.
 */

int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
